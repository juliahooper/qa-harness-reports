# Jentic Onboarding Evaluation [PASS]

## Scorecard

| Metric | Value |
|--------|-------|
| Tasks passed | 5/5 |
| Tasks skipped | 0/5 |
| Tasks blocked | 0/5 |
| Duration | 1m 18s |
| Cost | $0.31 |
| Blockers found | 0 |

## Journey

Agent successfully completed all five onboarding goals on the Jentic platform. Registration and authentication were seamless with CLI automation; the agent then discovered the test API, requested toolkit access (which was auto-approved), and made a successful proxied request through the broker with credential injection. All verification checks passed.

## Key Friction Points

- CLI command naming confusion: `jentic api` vs `jentic apis` (singular vs plural)
- API reference format validation error required full vendor/name/version syntax; incomplete references rejected
- Access request action validation rejected `--provision` flag; correct action is `bind` or `grant`
- API spec output minimal with no visible security definitions; agent had to infer authentication model
- `jentic apis inspect` help text unclear about required command signature and parameters
- Initial broker endpoint resolution used wrong host (172.28.0.5); localhost default worked

## Unlogged Insights

- Access request auto-approval workflow is efficient but agent had no visibility into approval criteria or SLA
- Token refresh after toolkit binding approval is a required manual step; could be automated
- Broker credential injection is transparent to user but agent had no way to verify which credentials were injected
- No explicit feedback on which toolkit binding was activated; agent inferred from `jentic access list` output
- CLI defaults (localhost broker, http scheme) are convenient for local dev but may mask production configuration issues

## Task Results

### [PASS] register-agent


### [PASS] authenticate


### [PASS] find-credential

**DOCS**
- Agent struggled with command syntax; tried `jentic api` (doesn't exist), `--query` flag (unsupported), and incomplete API references. Help text for `jentic apis inspect` was unclear.

### [PASS] add-to-toolkit

**MISC**
- Agent encountered validation error when attempting `--provision` action with incorrect payload structure; recovered by using correct `bind` action.

### [PASS] make-proxied-request

**MISC**
- Initial attempt failed with connection refused to 172.28.0.5:8100 (incorrect host); agent recovered by using localhost default.

## Issues Found

| # | Level | Task | Description |
|---|-------|------|-------------|
| 1 | **docs** | find-credential | Agent struggled with command syntax; tried `jentic api` (doesn't exist), `--query` flag (unsupported), and incomplete API references. Help text for `jentic apis inspect` was unclear. |
| 2 | **misc** | add-to-toolkit | Agent encountered validation error when attempting `--provision` action with incorrect payload structure; recovered by using correct `bind` action. |
| 3 | **misc** | make-proxied-request | Initial attempt failed with connection refused to 172.28.0.5:8100 (incorrect host); agent recovered by using localhost default. |

## Run Metadata

| Field | Value |
|-------|-------|
| Run ID | `019ff184-469c-7e71-817c-d1941689d9fb` |
| Timestamp | 2026-08-11T15:51:53 |
| Model | eu.anthropic.claude-haiku-4-5-20251001-v1:0 |
| Config | `products/jentic.yaml` |
| Duration | 1m 18s |
| Cost | $0.3076 |
| Outcome | succeeded |
