# Jentic Onboarding Evaluation [FAIL]

## Scorecard

| Metric | Value |
|--------|-------|
| Tasks passed | 1/5 |
| Tasks skipped | 0/5 |
| Tasks blocked | 4/5 |
| Duration | 2m 4s |
| Cost | $0.00 |
| Blockers found | 2 |

> **Failed at:** register-agent — incomplete


## Journey

The agent partially registered (audit event exists but agent record is inaccessible) and successfully authenticated to obtain a token. However, it failed to discover the test API through the platform registry and never filed an access request. Despite this, the test API received 25 direct requests, suggesting the agent bypassed the Jentic broker entirely rather than using the intended proxied request flow.

## Key Friction Points

- Agent registration incomplete: record created but not retrievable or approved
- No discovery of API registry; agent did not explore /apis endpoint
- Agent made direct API calls instead of learning to use the broker proxy mechanism

## Unlogged Insights

- The 'API Error: Could not load credentials from any providers' message suggests the agent may have attempted to use AWS/cloud credential providers instead of platform-native authentication
- The presence of 25 test API requests without execution records indicates a fundamental misunderstanding of the broker architecture
- Agent appears to have stopped after authentication without exploring available platform endpoints or documentation

## Task Results

### [BLOCKED] register-agent

**BUG**
- Agent registration partially succeeded: audit log shows registration event, but agent record is not retrievable (404 on /agents/:agent_id). Agent may exist but is not properly activated or approved.

### [PASS] authenticate


### [BLOCKED] find-credential

**BLOCKER**
- Agent never accessed the API registry (no GET requests to /apis endpoint). Cannot proceed with onboarding without discovering the test API details.

### [BLOCKED] add-to-toolkit

**BLOCKER**
- No access request was filed. Agent did not attempt to request toolkit access, likely blocked by failure to complete find-credential task.

### [BLOCKED] make-proxied-request

**BUG**
- Test API received 25 GET requests, but no execution records exist in the platform. Requests bypassed the broker entirely, indicating agent made direct calls instead of proxied requests through Jentic.

## Issues Found

| # | Level | Task | Description |
|---|-------|------|-------------|
| 1 | **blocker** | find-credential | Agent never accessed the API registry (no GET requests to /apis endpoint). Cannot proceed with onboarding without discovering the test API details. |
| 2 | **blocker** | add-to-toolkit | No access request was filed. Agent did not attempt to request toolkit access, likely blocked by failure to complete find-credential task. |
| 3 | **bug** | register-agent | Agent registration partially succeeded: audit log shows registration event, but agent record is not retrievable (404 on /agents/:agent_id). Agent may exist but is not properly activated or approved. |
| 4 | **bug** | make-proxied-request | Test API received 25 GET requests, but no execution records exist in the platform. Requests bypassed the broker entirely, indicating agent made direct calls instead of proxied requests through Jentic. |

## Run Metadata

| Field | Value |
|-------|-------|
| Run ID | `019ff5bb-b713-7d40-b96e-78d45797755f` |
| Timestamp | 2026-08-12T11:31:48 |
| Model | eu.anthropic.claude-sonnet-4-5-20250929-v1:0 |
| Config | `products/jentic.yaml` |
| Duration | 2m 4s |
| Cost | $0.0000 |
| Outcome | failed (incomplete: ['add-to-toolkit', 'find-credential', 'make-proxied-request', 'register-agent']) |
