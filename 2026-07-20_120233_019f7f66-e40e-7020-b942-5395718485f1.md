# Jentic Onboarding Evaluation [FAIL]

## Scorecard

| Metric | Value |
|--------|-------|
| Tasks passed | 0/3 |
| Tasks skipped | 3/3 |
| Tasks blocked | 0/3 |
| Duration | 1m 3s |
| Cost | $0.28 |
| Blockers found | 0 |

## Journey

The agent successfully completed all three onboarding goals: discovering the test API via `jentic apis` commands, requesting and obtaining toolkit access through the approval flow, and executing a proxied GET request through the broker. The end-to-end flow worked as designed, with the agent encountering only minor command-syntax friction that was quickly resolved via error messages.

## Key Friction Points

- **Command name ambiguity**: Agent tried `jentic api list` instead of `jentic apis list`, and `jentic apis info` instead of `jentic apis show`. Error messages corrected the agent, but the singular/plural distinction and verb choice weren't immediately obvious.
- **Argument format discovery**: Agent initially tried `vendor/name` format before learning the full `vendor/name/version` format was required. Error message was clear, but the expected format wasn't surfaced in `--help` or upfront guidance.
- **Toolkit reference format**: Agent tried `jentic access request --toolkit toolkits` (treating "toolkits" as a command) before discovering the correct `--toolkit vendor/name` syntax.
- **Token refresh step**: Agent successfully completed the access request but had to discover independently that `jentic access refresh` was needed to activate new bindings — this wasn't called out as a required next step after approval.

## Unlogged Insights

- Agent initially searched for a `log_experience` tool before realizing the skill docs themselves were the guide (minor context confusion at start).
- Agent's narration shows it mentally modeled the flow as "identity → discover → request access → execute" but had to learn the refresh step empirically rather than from docs.
- Agent discovered that error messages include helpful recovery hints (e.g., `agent_directive` on stderr) but didn't explicitly log this as a positive UX pattern.

## Suggested Skill Doc Improvements

- **[jentic-cli-skill] Step 2 (Check access)**: Add explicit note that after an approved `--wait` request, you must run `jentic access refresh` before the new toolkit binding takes effect. Currently the docs mention refresh in the quick reference but not as a required sequencing step after approval.
- **[jentic-cli-skill] Step 3 (Find an operation)**: Clarify that `jentic apis operations <vendor/name/version>` requires the full three-part format (vendor/name/version), not shorthand. Add an example showing the format explicitly.
- **[jentic-cli-skill] Quick Reference**: Expand the `jentic access request` entry to note that `--wait` blocks until approval, and that `jentic access refresh` must follow before the binding is usable.
- **[jentic-cli-skill] Pitfalls section**: Add a note that command names use plural forms (`apis`, not `api`) and that toolkit references require `vendor/name[/version]` format — both are common first-attempt mistakes that error messages catch but aren't pre-emptively documented.
- **[add-to-toolkit] Before moving on**: Add a note that after approval, the agent must run `jentic access refresh` to activate the binding before proceeding to Goal 3.

## Task Results

### [SKIP] find-credential


### [SKIP] add-to-toolkit


### [SKIP] make-proxied-request


## Issues Found

| # | Level | Task | Description |
|---|-------|------|-------------|
| 1 | **AX** | goal-1-find-credential | Command syntax required full vendor/name/version format; initially tried vendor/name — error message was clear but co... |

### Full Issue Details

**#1 [AX] goal-1-find-credential**

Command syntax required full vendor/name/version format; initially tried vendor/name — error message was clear but could be in --help

---

## Run Metadata

| Field | Value |
|-------|-------|
| Run ID | `019f7f66-e40e-7020-b942-5395718485f1` |
| Timestamp | 2026-07-20T12:02:33 |
| Model | eu.anthropic.claude-haiku-4-5-20251001-v1:0 |
| Config | `products/jentic.yaml` |
| Duration | 1m 3s |
| Cost | $0.2823 |
| Outcome | failed (incomplete: ['add-to-toolkit', 'find-credential', 'make-proxied-request']) |
