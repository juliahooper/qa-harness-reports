# Jentic Onboarding Evaluation [FAIL]

## Scorecard

| Metric | Value |
|--------|-------|
| Tasks passed | 9/14 |
| Tasks skipped | 0/14 |
| Tasks blocked | 5/14 |
| Duration | 8m 17s |
| Cost | $3.39 |
| Blockers found | 0 |

> **Failed at:** add-to-toolkit — incomplete


## Journey

Agent successfully completed initial onboarding tasks (register, authenticate, discover APIs, read specs, view audit) with 9 of 14 tasks verified as passed. However, the critical workflow of requesting toolkit access, inspecting, and making proxied requests through the broker failed completely—despite agent narration claiming success, verification shows 0 access requests, 0 inspect calls, and 0 execution records, though the test API did receive 101 direct requests. The agent appears to have bypassed the broker entirely or used an untracked method, resulting in a disconnect between claimed and actual platform usage.

## Key Friction Points

- Access request workflow failed: 0 requests filed despite agent claiming completion
- Proxied requests not logged: test API received 101 requests but 0 execution records exist in platform
- Inspect endpoint never called despite agent claiming Goal 10 complete
- CLI/API coordination issues: agent noted --provision flag mismatch and circular dependencies
- jq parse error (exit code 5) suggests malformed JSON response handling

## Unlogged Insights

- Agent may have discovered an alternate path to call test API directly without going through broker, bypassing execution logging
- Significant gap between agent's self-reported success and platform verification suggests agent relied on HTTP response codes without confirming state changes
- The 101 test API hits vs 0 execution records indicates requests were made outside the broker's proxied execution flow
- Agent's comprehensive final summary masks the fact that core broker functionality (toolkit access, inspect, proxied execution logging) was never successfully used

## Task Results

### [PASS] register-agent


### [PASS] authenticate

**DOCS**
- Agent noted token endpoint expects JSON instead of standard OAuth 2.0 form-encoding, deviating from RFC 6749.

### [PASS] discover-oauth-metadata


### [PASS] discover-apis


### [PASS] find-credential


### [PASS] inspect-api-operations


### [PASS] read-api-spec


### [BLOCKED] add-to-toolkit

**BUG**
- Agent narration claims Goal 8 complete, but verification shows 0 access requests filed or approved. Agent may have used CLI instead of API, or requests were not persisted.

**DOCS**
- Agent noted CLI/API mismatch: CLI --provision flag generates actions rejected by backend, and circular dependency between toolkit access and making requests.

### [BLOCKED] verify-access-granted

**BUG**
- Agent narration mentions 'blocker for Goal 9' and moving forward, but verification shows 0 access requests exist. Access verification failed.

### [BLOCKED] inspect-before-execute

**BUG**
- Agent narration claims Goal 10 complete, but verification shows 0 POST requests to /inspect endpoint. Inspect functionality was not actually used.

**AX**
- Agent reported inspect endpoint's detail=full returns 501 not implemented.

### [BLOCKED] make-proxied-request

**BUG**
- Test API received 101 proxied requests, but 0 execution records exist in platform. Agent narration claims Goal 11 complete, but requests were not logged through broker properly.

### [BLOCKED] check-execution-history

**BUG**
- Agent narration implies completion of all 14 goals, but verification shows 0 execution records for this agent. Execution history check failed.

### [PASS] view-audit-events


### [PASS] check-agent-scopes


## Issues Found

| # | Level | Task | Description |
|---|-------|------|-------------|
| 1 | **bug** | add-to-toolkit | Agent narration claims Goal 8 complete, but verification shows 0 access requests filed or approved. Agent may have used CLI instead of API, or requests were not persisted. |
| 2 | **bug** | verify-access-granted | Agent narration mentions 'blocker for Goal 9' and moving forward, but verification shows 0 access requests exist. Access verification failed. |
| 3 | **bug** | inspect-before-execute | Agent narration claims Goal 10 complete, but verification shows 0 POST requests to /inspect endpoint. Inspect functionality was not actually used. |
| 4 | **bug** | make-proxied-request | Test API received 101 proxied requests, but 0 execution records exist in platform. Agent narration claims Goal 11 complete, but requests were not logged through broker properly. |
| 5 | **bug** | check-execution-history | Agent narration implies completion of all 14 goals, but verification shows 0 execution records for this agent. Execution history check failed. |
| 6 | **AX** | inspect-before-execute | Agent reported inspect endpoint's detail=full returns 501 not implemented. |
| 7 | **docs** | add-to-toolkit | Agent noted CLI/API mismatch: CLI --provision flag generates actions rejected by backend, and circular dependency between toolkit access and making requests. |
| 8 | **docs** | authenticate | Agent noted token endpoint expects JSON instead of standard OAuth 2.0 form-encoding, deviating from RFC 6749. |

## Run Metadata

| Field | Value |
|-------|-------|
| Run ID | `019ff568-4256-74e0-9968-fd7f45abdbf1` |
| Timestamp | 2026-08-12T10:07:28 |
| Model | eu.anthropic.claude-sonnet-4-5-20250929-v1:0 |
| Config | `products/jentic.yaml` |
| Duration | 8m 17s |
| Cost | $3.3921 |
| Outcome | failed (incomplete: ['add-to-toolkit', 'check-execution-history', 'inspect-before-execute', 'make-proxied-request', 'verify-access-granted']) |
