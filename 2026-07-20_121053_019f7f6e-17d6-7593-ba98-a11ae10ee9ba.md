# Jentic Onboarding Evaluation [FAIL]

## Scorecard

| Metric | Value |
|--------|-------|
| Tasks passed | 0/3 |
| Tasks skipped | 3/3 |
| Tasks blocked | 0/3 |
| Duration | 1m 28s |
| Cost | $0.29 |
| Blockers found | 0 |

## Journey

The agent successfully registered, discovered the test API (`jentic-test/test-api/1.0.0`), filed and received approval for toolkit access, and executed two proxied GET requests through the broker—one using URL format and one using operation ID. All three goals completed without blocking failures, though the agent encountered multiple command-syntax errors during exploration.

## Key Friction Points

- **Command discovery was trial-and-error**: Agent tried `jentic api`, `jentic describe`, `jentic toolkits` before finding the correct commands (`jentic apis`, `jentic apis show`, `jentic access request`). The skill doc lists commands in reference format but doesn't clearly signal the discovery path or common mistakes.
- **API reference format was unclear**: Agent initially tried `jentic-test/test-api` (missing version) and received a cryptic error message before correcting to `jentic-test/test-api/1.0.0`. The error message itself didn't guide toward the solution.
- **Operation ID discovery lacked clarity**: Agent had to infer that `getRoot` was the operation ID from the spec output; the skill doc mentions `operation_id` from `search`/`apis operations` but doesn't explain how to extract it from a spec or what format to expect.

## Unlogged Insights

- Agent discovered that both `operation_id` and `METHOD:URL` formats work for `execute`, but had to test both to confirm—the skill doc mentions both but doesn't emphasize they're equivalent or when to prefer one.
- Agent inferred that `jentic access refresh` was necessary after approval by observing the token hadn't changed; the skill doc mentions refresh in the quick reference but not in the Goal 2 workflow.
- Agent used `jq` to parse JSON and hit a "command not found" error, revealing an undocumented dependency or environment assumption (though this is outside the skill doc scope).

## Suggested Skill Doc Improvements

- **Toolkit binding terminology**: The skill doc uses "toolkit binding" and "toolkit" interchangeably in places, and the agent had to infer that `jentic access refresh` was needed after approval to activate the binding—this step wasn't explicitly called out in Goal 2.
- **[jentic-cli-skill] Clarify the command discovery path in Section 3**: Add a note that `jentic apis list` shows available APIs, then `jentic apis show <vendor/name/version>` displays details. Explicitly warn against `jentic api` (common mistake) and show the correct form early.
- **[jentic-cli-skill] Add expected format and error recovery for API references**: In Section 3, after the `import` example, add: "API references must include version: `vendor/name/version`. If you see 'expected vendor/name/version', check the full reference with `jentic apis list`."
- **[jentic-cli-skill] Expand Section 4 (Inspect) with operation ID extraction**: Add an example showing how to extract `operation_id` from `jentic apis operations` output and pass it to `inspect`, e.g., `jentic inspect <operation_id>` vs. the `METHOD URL` form. Clarify that both resolve the same operation.
- **[add-to-toolkit] Add explicit refresh step after approval**: In the "Before moving on" section, add an action: "After approval is granted, run `jentic access refresh` to update your token with the new toolkit binding. Verify with `jentic access whoami` that the toolkit now appears under 'Toolkit Bindings'."
- **[jentic-cli-skill] Add inline examples for common command patterns**: In the Quick Reference, add one-line examples for `jentic apis show` and `jentic apis operations` so agents don't have to guess the syntax.
- **[jentic-cli-skill] Clarify broker target configuration for local installs earlier**: Move the local broker configuration note (currently buried in Section 5) to Section 1 or Prerequisites, since DNS errors on first `execute` are a common blocker.

## Task Results

### [SKIP] find-credential


### [SKIP] add-to-toolkit


### [SKIP] make-proxied-request


## Run Metadata

| Field | Value |
|-------|-------|
| Run ID | `019f7f6e-17d6-7593-ba98-a11ae10ee9ba` |
| Timestamp | 2026-07-20T12:10:53 |
| Model | eu.anthropic.claude-haiku-4-5-20251001-v1:0 |
| Config | `products/jentic.yaml` |
| Duration | 1m 28s |
| Cost | $0.2880 |
| Outcome | failed (incomplete: ['add-to-toolkit', 'find-credential', 'make-proxied-request']) |
