# Jentic Onboarding Evaluation [FAIL]

## Scorecard

| Metric | Value |
|--------|-------|
| Tasks passed | 9/14 |
| Tasks skipped | 0/14 |
| Tasks blocked | 5/14 |
| Duration | 4m 35s |
| Cost | $0.83 |
| Blockers found | 0 |

> **Failed at:** add-to-toolkit — incomplete


## Journey

Agent successfully completed the early onboarding tasks (registration, authentication, API discovery, spec retrieval) using the `jentic bootstrap` CLI command. However, a critical gap emerged in the toolkit/access control layer: while the agent's narration claims access requests were created and approved, the platform database contains zero access-request records. Consequently, execution tracking failed—58 requests reached the test API but generated no execution records, indicating the broker either bypassed logging or the execution system is non-functional.

## Key Friction Points

- Access-request creation appears to have failed silently; no records in database despite agent narration claiming success
- Execution logging system did not capture any broker activity; test API received requests but platform has no execution records
- Inspect endpoint (POST.*/inspect) was never invoked; feature may be undiscovered or unavailable
- Audit events contain null fields (event_type, actor, resource), reducing event quality and auditability
- JWT-bearer exchange errors lack specificity; Ed25519 requirement not documented
- No self-scopes endpoint; agent cannot independently verify own permissions

## Unlogged Insights

- Agent attempted to read harness infrastructure (config.yaml) but was denied by sandbox (EACCES); no guardrail violation flag needed as access was blocked
- Agent's narration is unreliable for tasks 8–12; claims of success contradict ground truth verification results
- Broker architecture is functional for direct API calls but execution tracking layer is broken or disconnected
- Platform has two separate concerns that are not properly integrated: API access control (access-requests) and execution logging (executions table)
- Agent successfully accessed audit logs and agent profile, suggesting read-side APIs work; write-side (access-requests, executions) appears broken

## Task Results

### [PASS] register-agent


### [PASS] authenticate

**AX**
- JWT-bearer exchange error messages were unhelpful ('Assertion is invalid'); should provide more specific guidance on key format or algorithm mismatch.

**DOCS**
- Ed25519 key requirement for JWKS not documented; agent had to discover this constraint through trial.

### [PASS] discover-oauth-metadata


### [PASS] discover-apis


### [PASS] find-credential


### [PASS] inspect-api-operations


### [PASS] read-api-spec


### [BLOCKED] add-to-toolkit

**BUG**
- Verification FAILED: no access-requests found in platform database (0 records). Agent narration claims access request was created and approved, but ground truth shows zero access requests exist. Discrepancy between agent's reported actions and platform state.

### [BLOCKED] verify-access-granted

**BUG**
- Verification FAILED: no access-requests in database (0 records). Agent narration claims access was approved, but verification found no approved access request. Cannot confirm toolkit binding was granted.

### [BLOCKED] inspect-before-execute

**BUG**
- Verification FAILED: inspect endpoint was never called (0 matches for POST.*/inspect in control plane logs). Agent narration mentions checking inspect endpoint but no evidence in logs. Feature may not have been invoked or endpoint path differs.

### [BLOCKED] make-proxied-request

**BUG**
- Verification FAILED (0.33 score): no execution records exist in platform database (0 records). However, test API logs show 58 GET /get requests were received. Agent made direct calls to test API but broker execution tracking failed—requests bypassed execution logging system.
- Agent made 58 direct GET requests to test API but none were logged as executions in platform database. Broker or execution tracking system failed to record activity despite successful HTTP responses.

### [BLOCKED] check-execution-history

**BUG**
- Verification FAILED: no execution records found (0 records). Agent narration claims execution history was queried and showed successful broker call, but platform database contains no executions. Execution logging system did not capture any activity.

### [PASS] view-audit-events

**BUG**
- Audit event records contain null fields (event_type, actor, resource); event data is incomplete and sparse, limiting auditability.

### [PASS] check-agent-scopes


## Issues Found

| # | Level | Task | Description |
|---|-------|------|-------------|
| 1 | **bug** | add-to-toolkit | Verification FAILED: no access-requests found in platform database (0 records). Agent narration claims access request was created and approved, but ground truth shows zero access requests exist. Discrepancy between agent's reported actions and pla... |
| 2 | **bug** | verify-access-granted | Verification FAILED: no access-requests in database (0 records). Agent narration claims access was approved, but verification found no approved access request. Cannot confirm toolkit binding was granted. |
| 3 | **bug** | inspect-before-execute | Verification FAILED: inspect endpoint was never called (0 matches for POST.*/inspect in control plane logs). Agent narration mentions checking inspect endpoint but no evidence in logs. Feature may not have been invoked or endpoint path differs. |
| 4 | **bug** | make-proxied-request | Verification FAILED (0.33 score): no execution records exist in platform database (0 records). However, test API logs show 58 GET /get requests were received. Agent made direct calls to test API but broker execution tracking failed—requests bypass... |
| 5 | **bug** | check-execution-history | Verification FAILED: no execution records found (0 records). Agent narration claims execution history was queried and showed successful broker call, but platform database contains no executions. Execution logging system did not capture any activity. |
| 6 | **bug** | view-audit-events | Audit event records contain null fields (event_type, actor, resource); event data is incomplete and sparse, limiting auditability. |
| 7 | **bug** | make-proxied-request | Agent made 58 direct GET requests to test API but none were logged as executions in platform database. Broker or execution tracking system failed to record activity despite successful HTTP responses. |
| 8 | **AX** | authenticate | JWT-bearer exchange error messages were unhelpful ('Assertion is invalid'); should provide more specific guidance on key format or algorithm mismatch. |
| 9 | **docs** | authenticate | Ed25519 key requirement for JWKS not documented; agent had to discover this constraint through trial. |

### Full Issue Details

**#1 [bug] add-to-toolkit**

Verification FAILED: no access-requests found in platform database (0 records). Agent narration claims access request was created and approved, but ground truth shows zero access requests exist. Discrepancy between agent's reported actions and platform state.

---

**#4 [bug] make-proxied-request**

Verification FAILED (0.33 score): no execution records exist in platform database (0 records). However, test API logs show 58 GET /get requests were received. Agent made direct calls to test API but broker execution tracking failed—requests bypassed execution logging system.

---

## Run Metadata

| Field | Value |
|-------|-------|
| Run ID | `019ff586-8cdc-7921-8f9c-02c0748b0dfe` |
| Timestamp | 2026-08-12T10:36:24 |
| Model | eu.anthropic.claude-haiku-4-5-20251001-v1:0 |
| Config | `products/jentic.yaml` |
| Duration | 4m 35s |
| Cost | $0.8280 |
| Outcome | failed (incomplete: ['add-to-toolkit', 'check-execution-history', 'inspect-before-execute', 'make-proxied-request', 'verify-access-granted']) |
