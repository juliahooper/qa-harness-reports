# Jentic Onboarding Evaluation [FAIL]

## Scorecard

| Metric | Value |
|--------|-------|
| Tasks passed | 13/14 |
| Tasks skipped | 0/14 |
| Tasks blocked | 1/14 |
| Duration | 3m 46s |
| Cost | $1.56 |
| Blockers found | 1 |

> **Failed at:** inspect-before-execute — incomplete


## Journey

The agent successfully completed 13 of 14 onboarding tasks, navigating registration, API discovery, access requests, and proxied execution. A critical blocker emerged when attempting to provision new credentials (HTTP 422 rejecting 'provision' action), forcing the agent to bind to an existing toolkit instead. The inspect-before-execute task failed silently—the agent believed it succeeded but verification shows the endpoint was never called, revealing both a usability issue with the inspect command and a gap in the agent's error detection.

## Key Friction Points

- Provisioning workflow broken: 'jentic access request --provision' fails with HTTP 422, only 'bind' or 'grant' actions accepted
- Inspect command has confusing argument format; multiple failed attempts with operation IDs and various syntaxes
- Command naming inconsistency: 'jentic api' vs 'jentic apis' causes initial confusion
- API reference format (vendor/name/version) not immediately obvious from error messages
- No CLI command for viewing execution history; requires direct API calls via curl

## Unlogged Insights

- Agent completed 13/14 tasks but missed that inspect-before-execute actually failed—narration claimed success while verification shows endpoint was never hit
- OpenAPI specs lack security scheme details, forcing agents to infer authentication from other sources
- Token refresh pattern (to activate new toolkit bindings) is not documented but was discovered through trial
- Audit log access appropriately restricted for agent accounts (no audit:read scope), but this could be clearer upfront

## Task Results

### [PASS] register-agent


### [PASS] authenticate


### [PASS] discover-apis

**AX**
- Command naming inconsistency: 'jentic api' fails, correct command is 'jentic apis'

### [PASS] find-credential

**DOCS**
- API reference format not obvious; agent tried 'jentic-test/test-api' before learning version is required (vendor/name/version)

### [PASS] inspect-api-operations


### [PASS] read-api-spec


### [PASS] discover-oauth-metadata


### [PASS] add-to-toolkit

**BLOCKER**
- HTTP 422 error when attempting 'jentic access request --provision'; platform rejects 'create' and 'provision' actions, only accepts 'bind' or 'grant'. Agent had to bind to existing toolkit instead of provisioning new credential.

### [PASS] verify-access-granted


### [PASS] make-proxied-request


### [BLOCKED] inspect-before-execute

**BUG**
- Agent narration claims inspect was completed, but verification shows inspect endpoint was never called (0 matches in logs)

**AX**
- Multiple failed attempts to use inspect command with various argument formats; error messages indicate confusion about expected input format

### [PASS] check-execution-history

**AX**
- No CLI command available for viewing executions; agent had to use curl directly

### [PASS] view-audit-events


### [PASS] check-agent-scopes


## Issues Found

| # | Level | Task | Description |
|---|-------|------|-------------|
| 1 | **blocker** | add-to-toolkit | HTTP 422 error when attempting 'jentic access request --provision'; platform rejects 'create' and 'provision' actions, only accepts 'bind' or 'grant'. Agent had to bind to existing toolkit instead of provisioning new credential. |
| 2 | **bug** | inspect-before-execute | Agent narration claims inspect was completed, but verification shows inspect endpoint was never called (0 matches in logs) |
| 3 | **AX** | discover-apis | Command naming inconsistency: 'jentic api' fails, correct command is 'jentic apis' |
| 4 | **AX** | inspect-before-execute | Multiple failed attempts to use inspect command with various argument formats; error messages indicate confusion about expected input format |
| 5 | **AX** | check-execution-history | No CLI command available for viewing executions; agent had to use curl directly |
| 6 | **docs** | find-credential | API reference format not obvious; agent tried 'jentic-test/test-api' before learning version is required (vendor/name/version) |

## Run Metadata

| Field | Value |
|-------|-------|
| Run ID | `019ffaac-571e-7542-a4d4-c6f1278dc668` |
| Timestamp | 2026-08-13T10:35:08 |
| Model | eu.anthropic.claude-sonnet-4-5-20250929-v1:0 |
| Config | `products/jentic.yaml` |
| Duration | 3m 46s |
| Cost | $1.5555 |
| Outcome | failed (incomplete: ['inspect-before-execute']) |
