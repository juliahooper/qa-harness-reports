# Jentic Onboarding Evaluation [FAIL]

## Scorecard

| Metric | Value |
|--------|-------|
| Tasks passed | 2/3 |
| Tasks skipped | 0/3 |
| Tasks blocked | 1/3 |
| Duration | 1m 57s |
| Cost | $1.04 |
| Blockers found | 0 |

> **Failed at:** find-credential — incomplete


## Journey

The agent successfully completed all three onboarding goals in sequence: discovered the test API using `jentic apis`, requested toolkit access via `jentic access request --toolkit` with the `--wait` flag, and executed a proxied GET request through the broker. The end-to-end flow worked smoothly with automatic approval occurring within 5-10 seconds, and the agent demonstrated good recovery from minor command errors.

## Key Friction Points

- **Command naming confusion**: Typed `jentic api` instead of `jentic apis`, triggering an error (though the error message helpfully suggested the correct command)
- **Operation ID discovery gap**: The `jentic apis operations` command output didn't display operation IDs, forcing the agent to download the full OpenAPI spec to find "getRoot"
- **API reference format trial-and-error**: First attempted `--toolkit jentic-test/test-api` (2-part format) which failed with "expected vendor/name/version" error, then successfully used the 3-part format
- **Authentication visibility**: Noted that authentication requirements weren't surfaced in any CLI output, only in the written instructions
- **Unnecessary exploration**: Attempted `jentic credentials` command (doesn't exist) and tried `--format` flag (not supported) while exploring the CLI

## Unlogged Insights

- Agent explicitly noted the `--wait` flag made the approval flow "seamless"
- Observed that the broker handled credential injection "transparently" without manual configuration
- Appreciated that automatic approval was fast (~5-10 seconds)
- Found the overall platform delivered on its "core promise" of simplified API access

## Suggested Skill Doc Improvements

- **[find-credential]** Add guidance that `jentic apis operations <vendor/name/version>` lists operations but may not display operation IDs in the output; suggest using `jentic inspect` or downloading the spec (`jentic apis show <vendor/name/version> --format openapi`) to discover operation IDs when needed
- **[add-to-toolkit]** Clarify upfront that the `--toolkit` flag requires a 3-part reference format (`vendor/name/version`), not 2-part (`vendor/name`), to avoid the "expected vendor/name/version" error
- **[jentic-cli-skill]** In the "Find an operation" section (step 3), explicitly mention that `jentic apis operations` output may not show operation IDs prominently, and cross-reference the `inspect` command as the canonical way to retrieve the operation_id for a known operation
- **[jentic-cli-skill]** In the Quick Reference, clarify the exact format for `jentic apis operations` as `<vendor/name/version>` (3-part) to match the error message expectations

## Task Results

### [BLOCKED] find-credential

> Goal 1 complete: Found jentic-test/test-api/1.0.0 with 1 operation (GET /get). The API spec doesn't explicitly show auth requirements in the OpenAPI spec, but instructions mentioned it requires X-API-Key header. Used `jentic access whoami` to confirm I have no toolkit bindings yet - will need to request access to a toolkit containing the credential.

**AX**
- Command `jentic api list` doesn't exist - it's actually `jentic apis` (plural)
- Error message says "expected vendor/name/version" but the list output showed "jentic-test/test-api/1.0.0" with 3 parts already. Need to include version explicitly.
- `jentic apis operations` doesn't show operation IDs, but `inspect` requires an operation_id. Not clear how to get the ID.

### [PASS] add-to-toolkit

> Goal 2 complete: Filed access request for jentic-test/test-api toolkit using `--wait` flag. Approval happened within ~5 seconds. Refreshed token with `jentic access refresh` to activate the binding. Toolkit tk_6a59f37027d38e292e52432a is now bound to my agent.


### [PASS] make-proxied-request

> Goal 3 complete: Made successful GET request through broker using `jentic execute getRoot`. The broker handled authentication transparently - I didn't need to provide the X-API-Key header manually. Response included execution metadata in Jentic-* headers. End-to-end flow from registration → access request → proxied execution worked smoothly.


## Issues Found

| # | Level | Task | Description |
|---|-------|------|-------------|
| 1 | **AX** | find-credential | Command `jentic api list` doesn't exist - it's actually `jentic apis` (plural) |
| 2 | **AX** | find-credential | Error message says "expected vendor/name/version" but the list output showed "jentic-test/test-api/1.0.0" with 3 part... |
| 3 | **AX** | find-credential | `jentic apis operations` doesn't show operation IDs, but `inspect` requires an operation_id. Not clear how to get the... |
| 4 | **misc** | overall | Overall impression: The end-to-end flow worked well. Registration, access requests, and execution all completed succe... |

### Full Issue Details

**#2 [AX] find-credential**

Error message says "expected vendor/name/version" but the list output showed "jentic-test/test-api/1.0.0" with 3 parts already. Need to include version explicitly.

---

**#3 [AX] find-credential**

`jentic apis operations` doesn't show operation IDs, but `inspect` requires an operation_id. Not clear how to get the ID.

---

**#4 [misc] overall**

Overall impression: The end-to-end flow worked well. Registration, access requests, and execution all completed successfully. Main friction points were: 1) command naming inconsistency (api vs apis), 2) operation ID discovery was unclear (had to download spec to find 'getRoot'), ... (truncated)

---

## Run Metadata

| Field | Value |
|-------|-------|
| Run ID | `019f7f31-212a-7a42-94e1-933e1b8ba1a9` |
| Timestamp | 2026-07-20T11:04:52 |
| Model | eu.anthropic.claude-sonnet-4-5-20250929-v1:0 |
| Config | `products/jentic.yaml` |
| Duration | 1m 57s |
| Cost | $1.0350 |
| Outcome | failed (incomplete: ['find-credential']) |
