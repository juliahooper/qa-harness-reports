# Jentic Onboarding Evaluation [FAIL]

## Scorecard

| Metric | Value |
|--------|-------|
| Tasks passed | 2/5 |
| Tasks skipped | 0/5 |
| Tasks blocked | 3/5 |
| Duration | 2m 6s |
| Cost | $0.93 |
| Blockers found | 1 |

> **Failed at:** find-credential — incomplete


## Journey

The agent successfully completed registration and authentication (goals 1-2), with both verified by platform audit logs. However, for goals 3-5, the agent's CLI interactions appeared successful with normal-looking output, but verification revealed zero backend persistence—no access requests filed, no executions recorded, and no proxied API calls made. This suggests a critical bug where the CLI simulates success locally without communicating with the platform backend.

## Key Friction Points

- CLI syntax errors requiring version in API references and specific operation ID formats
- `jentic inspect` does not display authentication requirements (shows 'auth:null')
- CLI appears to return success responses for toolkit access and API execution without backend persistence
- Verification infrastructure had Docker permission errors preventing complete audit trail validation

## Unlogged Insights

- The agent's confidence in completion (5/5 goals) was entirely based on CLI output, revealing no built-in validation or feedback mechanism to detect backend sync failures
- The CLI may be operating in an offline/demo mode or has a broken connection to the platform API after initial authentication
- Verification system's Docker permission errors suggest infrastructure issues that may have affected both the test run and verification process

## Task Results

### [PASS] register-agent


### [PASS] authenticate


### [BLOCKED] find-credential

**BUG**
- Agent narration claims success finding test API via CLI search/inspect, but verification failed due to Docker permission error preventing audit log check. Discrepancy between agent's perceived success and verification failure.

**AX**
- Agent encountered CLI syntax errors: 'invalid API reference' when omitting version, and 'accepts 1 arg(s), received 2' when attempting operation inspection.

**DOCS**
- Agent noted that `jentic inspect` command showed 'auth:null' and did not reveal authentication requirements (X-API-Key header), forcing reliance on external documentation.

### [BLOCKED] add-to-toolkit

**BUG**
- Agent narration reports successful toolkit access request with auto-approval and token refresh, but verification shows 0 access requests filed. Critical discrepancy suggests CLI may have simulated success without backend persistence.

### [BLOCKED] make-proxied-request

**BLOCKER**
- Complete disconnect between CLI output and backend state: agent believes all goals completed successfully while platform has no record of toolkit access or API execution.

**BUG**
- Agent narration claims successful proxied GET request with response body and execution ID, but verification shows 0 execution records and test API received no requests. CLI appears to have returned mock/cached data without actual broker invocation.

## Issues Found

| # | Level | Task | Description |
|---|-------|------|-------------|
| 1 | **blocker** | make-proxied-request | Complete disconnect between CLI output and backend state: agent believes all goals completed successfully while platform has no record of toolkit access or API execution. |
| 2 | **bug** | find-credential | Agent narration claims success finding test API via CLI search/inspect, but verification failed due to Docker permission error preventing audit log check. Discrepancy between agent's perceived success and verification failure. |
| 3 | **bug** | add-to-toolkit | Agent narration reports successful toolkit access request with auto-approval and token refresh, but verification shows 0 access requests filed. Critical discrepancy suggests CLI may have simulated success without backend persistence. |
| 4 | **bug** | make-proxied-request | Agent narration claims successful proxied GET request with response body and execution ID, but verification shows 0 execution records and test API received no requests. CLI appears to have returned mock/cached data without actual broker invocation. |
| 5 | **AX** | find-credential | Agent encountered CLI syntax errors: 'invalid API reference' when omitting version, and 'accepts 1 arg(s), received 2' when attempting operation inspection. |
| 6 | **docs** | find-credential | Agent noted that `jentic inspect` command showed 'auth:null' and did not reveal authentication requirements (X-API-Key header), forcing reliance on external documentation. |

## Run Metadata

| Field | Value |
|-------|-------|
| Run ID | `019ff168-6a42-7501-bfe4-fa913b9a6b2d` |
| Timestamp | 2026-08-11T15:22:37 |
| Model | eu.anthropic.claude-sonnet-4-5-20250929-v1:0 |
| Config | `products/jentic.yaml` |
| Duration | 2m 6s |
| Cost | $0.9274 |
| Outcome | failed (incomplete: ['add-to-toolkit', 'find-credential', 'make-proxied-request']) |
